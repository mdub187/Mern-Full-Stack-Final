DROP DATABASE IF EXISTS dogwoodCollars;
CREATE DATABASE dogwoodCollars;

\c dogwoodCollars;

CREATE TABLE user (
    id SERIAL PRIMARY KEY,
    first_name VARCHAR(255) NOT NULL,
    last_name VARCHAR(255) NOT NULL,
    email VARCHAR(255)
);

CREATE TABLE products (
    id SERIAL PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    description VARCHAR(255) NOT NULL,
    price INTEGER NOT NULL,
    size VARCHAR(255) NOT NULL,
    color VARCHAR(255)
);

INSERT INTO user (
    first_name,
    last_name,
    email
)
VALUES ('marc', 'weeks', 'marcw@gmail.com'
),
('Hamzah', 'Alsalmi', 'ha@gmail.com'),
('wills', 'lastname', ''),
('mike', 'lastname', 'mike@gmail.com');

INSERT INTO products (
    product_name,
    description,
    price,
    size,
    color
)
VALUES('A', '16mm BEAD', '24', 'X-SMALL', 'Black'    
);



-- PRICING
-- X-SMALL 16mm BEAD - UNDER 12° head circumference $24.00
-- SMALL- 20mm BEAD- 12'-15* head circumference $28.00
-- MEDIUM- 25mm BEAD- 15*-17* head circumference $38.00
-- LARGE- 30mm BEAD- ABOVE 17mm head circumference $48.00